# Allow running KDE Connect firewall rules without password
# Only permits these exact commands — nothing else
Nishant ALL=(root) NOPASSWD: /usr/bin/firewall-cmd --zone=nm-shared --add-service=kdeconnect, /usr/bin/firewall-cmd --zone=FedoraWorkstation --add-service=kdeconnect
